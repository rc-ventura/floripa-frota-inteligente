# SDD Final Review — 001-fontes-dados-simuladas
**Cycle:** 2 | **Date:** 20260715-1924 | **Status:** APPROVED

Dispatch do ciclo 1: **QA only** (Security manteve PASS HIGH sem re-review — o fix não toca superfície de segurança).

---

## QA Engineer Report — Cycle 2

**Escopo:** re-verificação do fix do bug HIGH (ciclo 1) + itens obrigatórios, sobre seeds regenerados. Suíte **10/10 verde** (`uv run pytest tests/test_gerador_dados.py -v`); geração **byte-idêntica em 2 runs** (`a76bd98…`).

### Coverage
- Novos testes presentes e válidos: `test_sem_alertas_espurios` (regressão do HIGH) e `test_endpoint_multas_erro_500` (caminho 500). Lacunas críticas do ciclo 1 fechadas: o invariante "não-demo longe dos limiares" e o erro 500 da API agora são exercidos.
- `test_sem_alertas_espurios` **reproduz fielmente o motor**: por (placa, tipo aplicável), km_desde = `km_atual − max(km_no_momento presente)`, dias_desde = evento mais recente por data; exige `km_desde < limite_km−antecedencia_km` **e** `dias_desde < limite_dias−antecedencia_dias`; ainda guarda "sem evento" e "âncora sem km". Como o mesmo cálculo acusou 4 cruzamentos no código antigo (ciclo 1), o teste **falharia antes** — coerente com o 2 failed → 10 passed.

### Bugs
- **HIGH (ciclo 1) — RESOLVIDO.** Fix em `gerador_dados.py:438-450`: calcula a âncora (maior km) por `(placa, _tipo_canonico)` **antes** de qualquer nulificação (sem risco de comparar `None`) e a exclui do pool elegível, junto com demo/garantia. Análise empírica do ciclo 1 re-executada nos seeds novos: **0 alertas espúrios** (era 4). Os 3 citados estão sadios: QSC3784 (idx6), ORR1H26 (idx21), INT6Y87 (idx33) — mesmas placas (seed determinística). Os 136 pares (placa,tipo) não-demo mantêm ≥1 evento com km.
- Nenhum bug novo introduzido.

### SDD Compliance
- ✅ Invariante de `data-model.md` (§ Cadastro base / Edge Cases) agora **satisfeito** e testado.
- ✅ Não-bloqueantes absorvidos (validados): API 500 com `detail` genérico + `logger.exception` (path só no log; teste confirma que não vaza); CLI com `parser.error` amigável (exit 2, sem traceback) e validação do pai de `--output`; guard de range `min(7500, max(1500, rodado-500))`; `condutores` documentado no data-model; volume "~250–300" (251 dentro); ADR-003 item 10; INCONSISTENCIAS.md § Anomalias protegidas.
- ❌ Faltando: nada. ⚠️ Desvios do ciclo 1: todos endereçados.

### Verdict: **PASS**
**Confidence:** HIGH
*(Fix correto e verificado empiricamente de forma independente — 0 cruzamentos km e dias; teste de regressão reproduz o motor e falha-antes/passa-depois; determinismo byte-a-byte; 10/10; itens obrigatórios e não-bloqueantes absorvidos. Sem necessidade de Devin — concordância com o fix.)*

---

## Tech Leader Verdict — Final

**Assessment:** HIGH demo-crítico do ciclo 1 resolvido e verificado independentemente (checagem própria do ciclo 1 re-executada contra os seeds regenerados: **0 alertas espúrios**; suíte **10/10**); a proteção da âncora por (placa,tipo) antes da nulificação (`gerador_dados.py:438-450`) fecha o gap de invariante sem tocar segurança nem determinismo — arquitetura sólida, ADR-003 agora alinhado (item 10) e coberto por teste de regressão do invariante negativo.

## Verdict: APPROVED

Todos os itens obrigatórios cumpridos; recomendados absorvidos (guard :251, API 500 genérico+log+teste, CLI `parser.error`, learning lesson criada e indexada). Security PASS HIGH mantido. Pronto para MR → `dev`.

### Next Cycle Dispatch
**Agents:** NONE — pipeline encerrado para a spec 001.

### Action Items Remanescentes
- Nenhum bloqueante. Ao abrir o MR para `dev`, ratificar o status dos ADRs 001–003 de "Proposta" → aceito (o merge é o gatilho de ratificação declarado no cabeçalho de cada ADR). Fora do escopo desta revisão de código.

---

## Next Cycle Dispatch
**Agents:** NONE
**Focus:** — (pipeline encerrado; próximo passo é o MR `feature/001-fontes-dados-simuladas` → `dev`)
